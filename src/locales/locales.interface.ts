export interface LocalesInterface {
  leavingStation: string;
  arrivingStation: string;
  from: string;
  to: string;
  noTrain: string;
  noStation: string;
  loadingApp: string;
  trains: string;
  stations: string;
  settings: string;
  searchStation: string;
}
